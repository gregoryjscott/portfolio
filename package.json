{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "lint-fix": "lerna run lint-fix",
    "types": "typesync --dry",
    "types-fix": "typesync",
    "www": "lerna run www"
  },
  "devDependencies": {
    "lerna": "^6.4.1",
    "typesync": "^0.9.2"
  }
}
